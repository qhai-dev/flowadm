import  { Outlet } from "react-router-dom";

export  default  function DatasetLayout(){
    return (
        <div>
            dataset details layout page
            <Outlet />
        </div>
    )
}
